---
- name: Add user to group, install Docker, Buildkite agent, and fonts
  hosts: all
  become: yes
  become_method: sudo

  roles:
    - docker
    - buildkite
    - fonts

  tasks:
    - name: Add user to sudo group
      user:
        name: rsherman
        groups: sudo
        append: yes
